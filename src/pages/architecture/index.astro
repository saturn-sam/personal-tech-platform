---
import KnowledgeIndexLayout from '@layouts/knowledge-index-layout.astro';
import { getPublishedKnowledgeAssetEntries } from '@lib/content/knowledge-assets';

const collection = 'architecture-guides';
const entries = await getPublishedKnowledgeAssetEntries(collection);
---

<KnowledgeIndexLayout collection={collection} entries={entries} />
